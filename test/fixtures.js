module.exports = function(){
    var fixtures = [];
    // Fixtures aren't in v2 format yet
    //fixtures.push(require('./fixtures/directions.json'));
    //fixtures.push(require('./fixtures/geolocation.json'));
    return fixtures;
};